import { getCookie, setCookie } from "typescript-cookie";
// setCookie("name", "value");

// export function setCookieCode(codeFromMail: string) {
//   setCookie("token", "codeFromMail");
// }

// export function getCookieCode() {
//   getCookie("token");
// }
